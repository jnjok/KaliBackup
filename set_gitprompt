# Git configuration
# Branch name in prompt
source ~/.git-prompt.sh
PS1='\[\e[1;32m[\]\W$(__git_ps1 " (%s)")]\$ \[\e[m\]'
export PROMPT_COMMAND='echo -ne "\033]0;${PWD/#$HOME/~}\007"'
# Tab completion for branch names
source ~/.git-completion.bash

